name: Set up Nix
description: >-
  Install Nix (Determinate Systems installer) and, when a cache name is
  provided, configure the Cachix binary cache. Shared by the CI and docs
  workflows so the installer/Cachix wiring lives in exactly one place.

# NOTE: `actions/checkout` intentionally lives in the caller, not here: a
# local composite action cannot check the repository out, because its own
# action.yml is not on disk until after checkout has run.

inputs:
  cachix-cache:
    description: Cachix cache name. When empty, the Cachix step is skipped.
    required: false
    default: ""
  cachix-auth-token:
    description: Cachix auth token (pass secrets.CACHIX_AUTH_TOKEN).
    required: false
    default: ""

runs:
  using: composite
  steps:
    - uses: DeterminateSystems/nix-installer-action@21a544727d0c62386e78b4befe52d19ad12692e3 # v17

    - name: Configure Cachix
      if: ${{ inputs.cachix-cache != '' }}
      uses: cachix/cachix-action@3ba601ff5bbb07c7220846facfa2cd81eeee15a1 # v16
      with:
        name: ${{ inputs.cachix-cache }}
        authToken: ${{ inputs.cachix-auth-token }}
